[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "backtester"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pandas", "numpy", "pandas_market_calendars>=4.0"]

[tool.setuptools.packages.find]
include = ["backtester*"]
